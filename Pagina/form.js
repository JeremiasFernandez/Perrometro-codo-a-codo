const form = document.getElementById('adoption-form');

form.addEventListener('submit', function (event) {
  event.preventDefault();
  function enviarFormulario() {
    alert("Espera un momento, pronto te llamaremos.");
  }
});